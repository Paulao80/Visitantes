@model IEnumerable<GuaritaVisitantes.DTO.Models.MotoristaDTO>

@{
    ViewBag.Title = "Motoristas";
}

<link href="~/Content/IndexLista.css" rel="stylesheet" />

<div class="card">
    <div class="card-title">
        @ViewBag.Title
    </div>

    <div class="card-body">
        <div id="table_div">
            <form action="~/Motoristas/Deletar" id="tabela01" method="post">
                <table id="table_data" class="display">
                    <thead>
                        <tr>
                            <th>#</th>
                            <th>Id</th>
                            <th>Nome</th>
                            <th>CNH/RG</th>
                            <th>Ações</th>
                        </tr>
                    </thead>
                    <tbody>
                        @{
                            foreach (var item in Model)
                            {
                                <tr>
                                    <td><input type="checkbox" name="ID" value="@item.MotoristaId"></td>
                                    <td>@item.MotoristaId</td>
                                    <td>@item.Nome</td>
                                    <td>@item.CnhRg</td>
                                    <td>
                                        <a class="btn btn-warning" href="/Transporte/Motoristas/Edit/@item.MotoristaId"><i class="fas fa-pencil-alt"></i></a>
                                        <a class="btn btn-primary" href="/Transporte/Motoristas/Details/@item.MotoristaId"><i class="fa fa-list"></i></a>
                                    </td>
                                </tr>
                            }
                        }
                    </tbody>
                </table>
            </form>
        </div>
    </div>

    <div class="card-footer">
    </div>
</div>
<br />
<br />
<br />
<br />
<div id="avisoDelete" class="aviso-del" style="display: none">
    <div class="aviso-title">
        <h1>Excluir</h1>
    </div>
    <div class="aviso-body">
        <p>Tem certeza que deseja excluir esses itens?</p>
    </div>
    <div class="aviso-footer">
        <button class="btn-aviso btn-aviso-sim">Sim</button>
        <button class="btn-aviso btn-aviso-nao">Não</button>
    </div>
</div>

<a class="btns btn-add" href="/Transporte/Motoristas/Create"><i class="fas fa-plus"></i></a>
<button class="btns btn-del"><i class="fas fa-trash-alt"></i></button>

<script src="~/Scripts/IndexLista.js"></script>